#the parameters used for LSH and bloom filter
#@R: if points' distance less than R, then the points are neighbour
#the dimension of points is 128. We assume the average distance between dimension of points is 5,
#then we compute (128*5^2)^0.5 = 56, so set r = 56
r=10
#@W: the w used by (av + b)/w
w=10
#@K: the number of lsh functions
k=4
#@L: the number of lsh hash tables
l=3
#@dimension: the dimensions of points
dimension=5
#@size: hash table size  
size=10000 


